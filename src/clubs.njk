---
layout: base.njk
permalink: /clubs/index.html
title: Clubs
eleventyComputed:
    description: "A directory of {{ clubs.length }} non-league clubs, sorted alphabetically"
---
{% from "macros.njk" import emoji %}
{% from "breadcrumb.macro.njk" import breadcrumb %}

<header class="container-fluid hero" id="club" data-theme="dark">
    <div class="container">
        <h1>Clubs</h1>
        <div class="grid">
            <p>
                <strong>A directory of {{ clubs.length }} non-league clubs, sorted alphabetically</strong>
            </p>
            <p></p>
        </div>
    </div>
</header>

<main class="container">
    <table role="grid">
        <thead>
            <tr>
                <th scope="col">Club</th>
                <th scope="col">Ground</th>
                <th scope="col">League</th>
            </tr>
        </thead>
        <tbody>
            {% for club in clubs | sort(attribute = "name") %}
                <tr>
                    <th>
                        <a href="/leagues/{{ club.memberOf.name | slugify }}/{{ club.name | slugify }}">{{ club.name }}</a>
                    </th>
                    <td>
                        <a href="/grounds/{{ club.location.areaServed.name | slugify }}/{{ club.location.name | slugify }}" class="secondary">{{ club.location.name }}</a><br/>
                    </td>
                    <td>
                        {{ club.memberOf.name }}
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

</main>
<footer class="container-fluid">
    <div class="container">
        <small>
            Photo by <a href="https://unsplash.com/@nickxshotz?utm_source=unsplash&utm_medium=referral&utm_content=creditCopyText">Nicholas Green</a> on <a href="https://unsplash.com/s/photos/soccer-crowd?utm_source=unsplash&utm_medium=referral&utm_content=creditCopyText">Unsplash</a>
        </small>
    </div>
</footer>