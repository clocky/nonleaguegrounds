---
layout: base.njk
permalink: /ground/index.html
eleventyComputed:
    title: "Football Conference Leagues"
    description: "A directory of {{ grounds.length }} English non-league grounds, sorted by capacity."
---

<header class="container-fluid hero" id="ground" data-theme="dark">
    <div class="container">
        <h1>Grounds</h1>
        <div class="grid">
            <h5>A directory of {{ grounds.length }} English non-league grounds, sorted by capacity.</h5>
            <p></p>
        </div>
    </div>
</header>

<main class="container">

    <table role="grid">
        <thead>
            <tr>
                <th scope="col">Pos</th>
                <th scope="col">Name</th>
                <th scope="col">Club</th>
                <th scope="col">Capacity</th>
            </tr>
        </thead>
        <tbody>
            {% for ground in grounds | sort(attribute = "capacity") | reverse %}
                <tr id="{{ ground.name | slugify }}">
                    <td valign="top">
                        {{ loop.index | ordinal }}
                    </td>
                    <td>
                        <a href="/ground/{{ ground.region | slugify }}/{{ ground.name | slugify }}/">
                            {{ ground.name }}
                        </a><br/>
                        <small>{{ ground.sponsor }}</small>
                    </td>
                    <td valign="top">
                        {% set club = "" %}
                        {% for c in clubs %}
                            {% if c.ground == ground.name %}
                                {{ c.name }}
                                {% set club = c %}
                            {% endif %}
                        {% endfor %}
                    </td>
                    <td valign="top">
                        {{ ground.capacity | commaNumber }}
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
</main>
<footer class="container-fluid">
    <div class="container">
        <small>Photo by <a href="https://unsplash.com/@fancycrave?utm_source=unsplash&utm_medium=referral&utm_content=creditCopyText">Fancy Crave</a> on <a href="https://unsplash.com/s/photos/soccer-ground?utm_source=unsplash&utm_medium=referral&utm_content=creditCopyText">Unsplash</a>
        </small>
    </div>
</footer>