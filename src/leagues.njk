---
permalink: /leagues/index.html
preload: "ezequiel-garrido-s6XWkKDKynY-unsplash.jpg"
date: git Last Modified
eleventyComputed:
    title: "Football Conference Leagues"
    description: "A directory of {{ leagues.length }} leagues in the English football conference, sorted by tier."
---
{% extends "./_layouts/base.njk" %}
{% from "unsplash.macro.njk" import unsplash %}
{% from "breadcrumb.macro.njk" import breadcrumb %}
{% from "hero.macro.njk" import hero %}

{%- block link %}
  <link rel="preload" as="image" href="{{ meta.hero['leagues'].src }}.jpg"/>
  <link rel="canonical" href="{{ meta.base }}/leagues">
{%- endblock %}

{%- block css %}{%- endblock %}

{% block header %}
  {{ hero("Leagues", "A directory of " + leagues.length + " leagues in the English football conference, sorted by tier", meta.hero['leagues'].src) }}
{% endblock %}

{% block breadcrumb %}
  {{- breadcrumb("Leagues") }}
{% endblock %}

{% block main %}
  <main class="container">
    <table class="{{ meta.class.table }}">
      <thead>
        <tr>
          <th scope="col">Tier</th>
          <th scope="col">Name</th>
          <th scope="col" class="is-hidden-mobile">Clubs</th>
        </tr>
      </thead>
      <tbody>
        {% for league in leagues | sort(attribute = "additionalProperty.value")%}
          <tr>
            <td>
              {{ league.additionalProperty.value | ordinal }}
            </td>
            <th scope="row">
              <a href="/leagues/{{ league.name | slugify }}">
                {{ league.alternateName }}
              </a>
            </th>
            <td class="is-hidden-mobile">
              {{ league.members | length }}
            </td>
          </tr>
        {% endfor %}
      </tbody>
    </table>
  </main>
  {{ unsplash(meta.hero["leagues"]) }}
{% endblock %}