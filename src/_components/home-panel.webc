<div :id="`${title.toLowerCase()}-panel`" class="py-12 px-8 shadow-md rounded-xl">
  <div class="w-6 h-6 mb-4">
    <slot name="icon"></slot>
  </div>
  <h3 @text="title" class="text-xl font-medium mb-2"></h3>
  <p class="font-base mb-8 text-slate-500 text-base">
    <slot name="description"></slot>
  </p>
  <p>
    <action-button :href="`/${title.toLowerCase()}/`" :label="`View all ${title.toLowerCase()} &rarr;`" :arrow="true">
    </action-button>
  </p>
</div>