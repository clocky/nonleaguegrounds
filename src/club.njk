---
layout: base.njk
pagination:
    data: clubs
    size: 1
    alias: club
permalink: "/{{ club.memberOf.name | slugify }}/{{ club.name | slugify }}/index.html"
eleventyComputed:
    title: "{{ club.name }} | {{ meta.title }}"
    description: "Information about {{ club.name }}, a {{club.keywords}} football club."
---
{% from "macros.njk" import emoji %}

<script type="application/ld+json">
  {{ club | dump | safe }}
</script>

<header class="container-fluid hero" id="{{ club.location.name | slugify }}" data-theme="dark">
  <div class="container" >
    <h1 id="{{ club.name | slugify }}">{{ club.name | replace(" ", "\n") | nl2br | safe}}</h1>
  </div>
</header>

<main class="container">
  <div class="grid">
    <section itemscope itemtype="https://schema.org/SportsTeam">
      <p>
        <strong itemprop="alternateName">
          {{- club.alternateName -}}
        </strong>
          is an <span itemprop="sport">{{ club.sport | lower }}</span> club, based in {{ club.location.address.addressLocality }}
        {%- if club.location.address.addressRegion -%}
            , {{ club.location.address.addressRegion }}
        {%- endif -%}.
        </p>
      <p>
          Founded {{ club.foundingDate | ago }} years ago in <span itemprop="foundingDate">{{ club.foundingDate }}</span>, the team currently compete in the
          <a href="/{{ club.memberOf.name | slugify }}">{{ club.memberOf.sponsor.name }}
          {{ club.memberOf.name }}</a>, 
            {%- if club.memberOf.additionalProperty %}
            the {{ club.memberOf.additionalProperty.@value | ordinal }} tier of English football
            {%- endif %}.
        </p>
      <p itemscope itemtype="https://schema.org/StadiumOrArena">
          They play their home matches at 
          <a href="/{{ club.areaServed.name | slugify }}/{{ club.location.name | slugify }}" itemprop="name">{{ club.location.name }}</a>,
          and are nicknamed <strong itemprop="slogan">"{{ club.slogan }}"</strong>.
        </p>
    </section>
    <aside>
      <table role="grid">
        <tbody>
          <tr>
            <th scope="row">Full name</th>
            <td>{{ club.alternateName }}</td>
          </tr>
          <tr>
            <th scope="row">Nickname</th>
            <td>
              {{ club.slogan }}
              <span class="emoji">{{emoji(club.slogan)}}</span>
            </td>
          </tr>
          <tr>
            <th scope="row">Founded</th>
            <td>
              <time datetime="YYYY">{{ club.foundingDate }}</time>
            </td>
          </tr>
          <tr>
            <th scope="row">Ground</th>
            <td>
              <a href="/{{ club.areaServed.name | slugify }}/{{ club.location.name | slugify }}/">
                {{ club.location.name }}
              </a>
            </td>
          </tr>
          <tr>
            <th scope="row">Capacity</th>
            <td>{{ club.location.maximumAttendeeCapacity | commaNumber }}</td>
          </tr>
          <tr>
            <th scope="row">League</th>
            <td>
              <a href="/{{ club.memberOf.name | slugify }}">
                {{ club.memberOf.name }}
              </a>
            </td>
          </tr>
          {% if club.url %}
            <tr>
              <th scope="row">Web</th>
              <td>
                <a href="{{ club.url }}/" target="_blank" rel="noopener">
                  {{ club.url.slice(8) }} ↗ </a>
              </td>
            </tr>
          {% endif %}
          {% if club.twitter %}
            <tr>
              <th scope="row">Twitter</th>
              <td>
                <a href="https://twitter.com/@{{ club.twitter }}" target="_blank">@{{ club.twitter | lower }} ↗ </a>
              </td>
            </tr>
          {% endif %}
        </tbody>
      </table>
    </aside>
  </div>

  <footer class="container">
    <small>Background image supplied by <a href="https://developers.google.com/maps/documentation/maps-static/overview" target="_blank">Google Maps</a>
    </small>
  </footer>
</main>