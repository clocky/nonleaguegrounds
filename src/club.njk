---
layout: base.njk
pagination:
    data: clubs
    size: 1
    alias: club
permalink: "/leagues/{{ club.memberOf.name | slugify }}/{{ club.name | slugify }}/index.html"
eleventyComputed:
    title: "{{ club.name }} | {{ meta.title }}"
    description: "Information about {{ club.name }}, a {{club.keywords}} football club."
---
{% from "macros.njk" import emoji %}
{% from "breadcrumb.macro.njk" import breadcrumb %}

<script type="application/ld+json">
  {{ club | dump | safe }}
</script>

<header class="container-fluid hero" id="{{ club.location.name | slugify }}" data-theme="dark">
  <div class="container">
    <h1 id="{{ club.name | slugify }}">{{ club.name | replace(" ", "\n") | nl2br | safe}}</h1>
  </div>
</header>

{{ breadcrumb("Leagues", club.memberOf.name, club.name) }}

<main class="container" >
  <div class="grid">
    <section>
      <p>
        <strong>{{ club.alternateName }}</strong> is a {{club.keywords}} football club
        &nbsp;based in {{ club.location.address.addressLocality }}, {{ club.location.address.addressRegion }}
      </p>
      {# <p>
        Founded in {{ club.foundingDate.@value }} and
        known informally as <strong>{{ club.slogan }}</strong>, 
        they compete in the <a href="/league/{{ club.memberOf.name | slugify }}/">{{ club.memberOf.name }}</a>, the {{ club.memberOf.additionalProperty.@value | ordinal }} tier of the English football league system. 
    </p> #}
      <p>
        {{ club.name }} play their home games at the {{ club.location.maximumAttendeeCapacity| commaNumber }}-capacity ground at 
          {% if club.location.sponsor %}
          <strong>{{ club.location.sponsor.name }}</strong>
        {% else %}
          <strong>
            <a href="/grounds/{{ club.areaServed.name | slugify }}/{{ club.location.name | slugify }}/">{{ club.location.name }}</a>
          </strong>
        {%- endif -%}
        {%- if ground.sponsor -%}
          , known by the official name of <strong>
            <a href="/grounds/{{ club.areaServed.name | slugify }}/{{ club.location.name | slugify }}">{{ ground.location.sponsor.name }}</a>
          </strong> for sponsorship reasons
          {%- endif -%}.
      </p>
    </section>
    <aside>
      <table role="grid">
        <tbody>
          <tr>
            <th>Full name</th>
            <td>{{ club.alternateName }}</td>
          </tr>
          <tr>
            <th>Nickname</th>
            <td>{{ club.slogan }}
              <span class="emoji">{{ emoji(club.slogan) }}</span>
            </td>
          </tr>
          {% if club.founded %}
            <tr>
              <th>Founded</th>
              <td>
                <time datetime="YYYY">{{ club.founded }}</time>
              </td>
            </tr>
          {% endif %}
          {% if club.location %}
            <tr>
              <th>Ground</th>
              <td>
                <a href="/grounds/{{ club.location.address.addressRegion | slugify }}/{{ club.location.name | slugify }}/">
                  {{ club.location.name }}
                </a>
              </td>
            </tr>
          {% endif %}
          {% if club.location.maximumAttendeeCapacity %}
            <tr>
              <th>Capacity</th>
              <td>{{ club.location.maximumAttendeeCapacity | commaNumber }}</td>
            </tr>
          {% endif %}
          <tr>
            <th>League</th>
            <td>
              <a href="/leagues/{{ club.memberOf.name | slugify }}/">
                {{ club.memberOf.name }}
              </a>
            </td>
          </tr>
          {% if club.url %}
            <tr>
              <th>Web</th>
              <td>
                <a href="{{ club.url }}/" target="_blank" rel="noopener">{{ club.url }} ↗ </a>
              </td>
            </tr>
          {% endif %}
          {% if club.twitter %}
            <tr>
              <th>Twitter</th>
              <td>
                <a href="https://twitter.com/@{{ club.twitter }}" target="_blank" rel="noopener">@{{ club.twitter | lower }} ↗ </a>
              </td>
            </tr>
          {% endif %}
        </tbody>
      </table>
    </aside>
  </div>

  <footer class="container">
    <small>Background image supplied by <a href="https://developers.google.com/maps/documentation/maps-static/overview" target="_blank">Google Maps</a>
    </small>
  </footer>
</main>