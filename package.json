{
  "name": "nonleaguegrounds-local",
  "version": "1.0.0",
  "description": "Database of British non-league grounds",
  "main": "index.html",
  "scripts": {
    "webhint": "npx hint -c .hintrc https://www.nonleaguegrounds.com",
    "watch:sass": "sass --no-source-map --watch src/sass:dist/css",
    "watch:eleventy": "ELEVENTY_ENV=development eleventy --serve",
    "build:sass": "sass --no-source-map src/sass:dist/css",
    "build:eleventy": "ELEVENTY_ENV=prod eleventy --quiet",
    "postbuild": "npm-run-all postbuild:css postbuild:map postbuild:sharp",
    "start": "npm-run-all build:sass --parallel watch:*",
    "build": "npm-run-all build:sass build:eleventy",
    "postbuild:css": "lightningcss --minify --targets '> 0.25%, not IE 11' dist/css/style.css -o dist/css/style.css",
    "postbuild:map": "lightningcss --minify --targets '> 0.25%, not IE 11' dist/css/maps.css -o dist/css/maps.css",
    "postbuild:sharp": "sharp resize 1280 -i dist/img/*.jpg -o dist/img --optimize"
  },
  "keywords": [
    "football",
    "soccer",
    "uk"
  ],
  "prettier": {
    "singleQuote": false,
    "trailingComma": "es5",
    "tabwidth": 2
  },
  "author": "Mark McLaughlin",
  "license": "MIT",
  "dependencies": {
    "@11ty/eleventy": "^1.0.2",
    "@11ty/eleventy-fetch": "^3.0.0",
    "@prettier/plugin-xml": "^2.2.0",
    "bootstrap": "^5.2.2",
    "comma-number": "^2.1.0",
    "dayjs": "^1.11.6",
    "distance-from": "^1.1.2",
    "dotenv": "^16.0.3",
    "js-yaml": "^4.1.0",
    "lightningcss-cli": "^1.16.0",
    "netlify-plugin-cache": "^1.0.3",
    "ordinal": "^1.0.3",
    "sass": "^1.55.0"
  },
  "devDependencies": {
    "gzipper": "^7.1.0",
    "npm-run-all": "^4.1.5",
    "prettier": "^2.7.1",
    "sharp-cli": "^3.0.0"
  }
}
